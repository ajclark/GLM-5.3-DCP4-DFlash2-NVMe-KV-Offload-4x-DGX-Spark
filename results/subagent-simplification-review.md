# Simplification review (Claude sub-agent, 2026-09-07)

Ranked: (1) drop the vLLM /metrics + /load plan, use the plugin's status line only (idle= is seconds since the last isend/irecv, exactly what makes commit safe; survives watcher restarts; /load needs a launcher flag that is not set; the finished-request counter misfires on one long generation); (2) shrink the watcher 83 -> ~25 lines (no throttling, backoff, failure counters, restore-on-vanish; replace the last with `spark-idle.sh --up --no-plugin` before launch); (3) delete metrics/HTTP; (4) delete abort (resume from prepared drops the gate); (5) delete per-connection threads and the port walk (one NCCL process per node); (6) delete --restore-after.
Keep: two-phase prepare/commit (a single verb races an arriving request and ends in a failed rank), parallel resume, ring verification on --up, "unreachable never idle", systemd Restart=always, loopback + SSH.
Final design, 12 lines: plugin TCP 127.0.0.1:5711 with status|prepare|commit|resume served inline; idle= and wanted=; spark-idle.sh reaches the plugin through ssh + /dev/tcp; --down = prepare x4 in parallel, all prepared -> commit x4 else resume x4 and exit 1, then sysfs off; --up = sysfs on, ring verification, resume x4 in parallel; watcher on spark-06c4 (systemd), every 10 s: status from all four, fewer than four answers -> hold; all active and min idle >= 5400 -> --down; any suspended and wanted=1 -> --up; no state across iterations; launcher sets NCCL_HOTPLUG_CTL_PORT=5711 and runs `spark-idle.sh --up --no-plugin` first; README states one NCCL process per node.
